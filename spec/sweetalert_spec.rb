require 'spec_helper'

describe Sweetalert do
  it 'has a version number' do
    expect(Sweetalert::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
